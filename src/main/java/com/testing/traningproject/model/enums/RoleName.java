package com.testing.traningproject.model.enums;

/**
 * Enum for Role names
 * Maps to: CHECK (name IN ('CUSTOMER', 'SERVICE_PROVIDER', 'ADMIN'))
 */
public enum RoleName {
    CUSTOMER,
    SERVICE_PROVIDER,
    ADMIN
}

