package com.testing.traningproject.model.enums;

/**
 * Enum for Refund status
 * Maps to: CHECK (status IN ('PENDING', 'APPROVED', 'REJECTED', 'COMPLETED'))
 */
public enum RefundStatus {
    PENDING,
    APPROVED,
    REJECTED,
    COMPLETED
}

