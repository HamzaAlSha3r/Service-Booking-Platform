package com.testing.traningproject.model.enums;

/**
 * Enum for Service type
 * Maps to: CHECK (service_type IN ('ONLINE', 'IN_PERSON', 'BOTH'))
 */
public enum ServiceType {
    ONLINE,
    IN_PERSON,
    BOTH
}
