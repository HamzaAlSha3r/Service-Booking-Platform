package com.testing.traningproject.model.enums;

/**
 * Enum for Subscription status
 * Maps to: CHECK (status IN ('ACTIVE', 'EXPIRED', 'CANCELLED'))
 */
public enum SubscriptionStatus {
    ACTIVE,
    EXPIRED,
    CANCELLED
}

