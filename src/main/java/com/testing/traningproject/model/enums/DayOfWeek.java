package com.testing.traningproject.model.enums;

/**
 * Enum for Days of the week
 * Maps to: CHECK (day_of_week IN ('MONDAY', 'TUESDAY', ...))
 */
public enum DayOfWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}

