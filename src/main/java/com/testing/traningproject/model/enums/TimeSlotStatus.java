package com.testing.traningproject.model.enums;

/**
 * Enum for Time Slot status
 * Maps to: CHECK (status IN ('AVAILABLE', 'BOOKED', 'BLOCKED'))
 */
public enum TimeSlotStatus {
    AVAILABLE,
    BOOKED,
    BLOCKED
}

